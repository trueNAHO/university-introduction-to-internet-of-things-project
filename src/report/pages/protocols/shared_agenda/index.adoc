= Shared Agenda

Considering the DB merely retrieves the shared agenda information, the flexible
REST API over HTTP protocol seems appropriate.

In this case, the DB sends HTTP GET requests to the shared agenda REST API.
